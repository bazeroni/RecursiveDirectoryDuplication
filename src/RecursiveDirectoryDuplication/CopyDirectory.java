// RecursiveDirectoryDuplication.java
// program for recursively creating, listing, and copying a directory from source to destination for CSCI 112
// last edited Feb. 20, 2022 by S. Gutierrez

package RecursiveDirectoryDuplication;

// iterates given array from ListDirectory() and recursively copies files to destination based on file on level vs file in sub-directory
public class CopyDirectory {

    // if it's a file, copy to destination

    // if else directory, recursively call CopyDirectory() on that directory

}
